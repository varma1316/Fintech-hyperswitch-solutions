apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: cart-service
  labels:
    app: cart-service
spec:
  type: ClusterIP
  ports:
    - port: 4003
      targetPort: 4003
      protocol: TCP
      name: http
  selector:
    app: cart-service
