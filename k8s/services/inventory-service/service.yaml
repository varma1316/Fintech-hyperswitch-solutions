apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: inventory-service
  labels:
    app: inventory-service
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
      name: http
  selector:
    app: inventory-service
